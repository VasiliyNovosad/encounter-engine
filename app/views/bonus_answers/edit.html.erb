<p>
  Редагування відповіді для бонуса "<%= @bonus.name %>" до завдання "<%= link_to @level.name, game_level_path(@game, @level) %>" гри "<%= link_to @game.name, game_path(@game) %>"
</p>

<br>

<%= form_for @bonus_answer, url: game_level_bonuse_bonus_answer_path(@game, @level, @bonus, @bonus_answer) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>

    <%= render 'form', f: f %>

    <%= f.submit "Редагувати", style: "width:170px" %>
    <em><%= link_to "(відмінити)", game_level_path(@game, @level) %></em>
<% end %>