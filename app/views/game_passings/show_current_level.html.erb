<h2>
  "<strong><%= @game.name %></strong>"
  <br>
  Завдання <%= @game.game_type == 'selected' ? @game_passing.current_level_position(@team_id) : @level.position %> із <%= @game.levels.count %>
</h2>
<br>

<%= render @game.game_type == 'panic' ? 'panic' : 'linear' %>

<!--
<% if current_user.captain? %>
    <%#= link_to "Зійти з дистанції", url: "game_passings/exit_game/#{@game_passing.game_id}" %>
<% end %>
-->
<% if @game.is_testing? %>
  <br>
  <%= link_to "Завершити тестування", finish_test_game_path(@game), method: :post %>
<% end %>
