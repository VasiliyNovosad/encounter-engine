<h1>Лог відповідей команди "<%= @team.name %>" по рівню "<%= @level.name %>" гри "<%= @game.name %>"</h1>

<strong>Вірний код:</strong>&nbsp;<%= @level.questions.first.correct_answer %>
<ul>
<% @logs.each do |log| %>
  <li><%= log.time.strftime("%Y-%m-%d %H:%M:%S") %>&nbsp;<%= log.answer %></li>
<% end %>
</ul>
