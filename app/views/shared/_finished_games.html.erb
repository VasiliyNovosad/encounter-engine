<% if finished_games.size > 0 %>
  <% finished_games.each do |game | %>
    <fieldset class="game-fieldset">
      <legend>
      </legend>
      <%= render 'shared/finished_game', game: game %>
    </fieldset>
    <br><br>
  <% end %>
<% end %>
