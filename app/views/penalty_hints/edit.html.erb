<p>
  Редагування штрафної підказки до завдання "<%= link_to @level.name, game_level_path(@game, @level) %>" гри "<%= link_to @game.name, game_path(@game) %>"
</p>

<br>

<%= form_for @penalty_hint, url: game_level_penalty_hint_path(@game, @level, @penalty_hint) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>  
  <%= render 'form', f: f %>
  <%= f.submit "Редагувати", class: 'btn btn-light' %>
  <em><%= link_to "(відмінити)", game_level_path(@game, @level, anchor: "penalty-hint-#{@penalty_hint.id}") %></em>
<% end %>
