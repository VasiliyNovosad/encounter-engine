<%= tinymce language: 'uk' %>
Нове завдання для гри <%= @level.game.name %><br>
<br>

<%= form_for @level, url: game_levels_path(@level.game) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>  
  <p><%= f.label :name, "Назва" %><%= f.text_field :name %></p>
  <p><%#= f.label :text, "Текст завдання" %><%= f.text_area :text, class: "tinymce" %></p>
  <p><%= f.label "Олімпійка?" %><%= f.check_box :olymp %></p>
  <p><%= f.label "Автозавершення через " %><%= f.text_field :complete_later_minutes %> хвилин</p>
  <p>
    <%= f.label :correct_answer, "Код" %><%= f.text_field :correct_answer %>
    <em>після створення Ви зможете додати більше кодів в завдання</em>
  </p>
  <%= f.submit "Додати завдання" %>
  <em><%= link_to "(відмінити)", game_path(@level.game) %></em>
<% end %>
