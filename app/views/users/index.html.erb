<h2>
  Профіль користувача <%= @current_user.nickname %>
  <% if @current_user.captain? %>
    (капітан команди <%= link_to @current_user.team.name, team_path(@current_user.team) %>)
  <% end %>
</h2>
<br>

<table>
  <tr><td width="150"><b>E-mail:</b></td>
      <td><%= current_user.email %></td></tr>
  <tr><td><b>Дата народження:</b></td>
      <td><%= current_user.date_of_birth %></td></tr>
  <tr><td><b>Номер ICQ:</b></td>
      <td><%= current_user.icq_number %></td></tr>
  <tr><td><b>Jabber ID:</b></td>
      <td><%= current_user.jabber_id %></td></tr>
  <% if current_user.captain? %>
    <tr><td><b>Контактний телефон:</b></td>
        <td><%= current_user.phone_number %></td></tr>
  <% end %>
</table>
<br>

<em><%= link_to "Редагувати профіль...", edit_user_path(current_user) %></em>