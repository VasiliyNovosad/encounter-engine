<h2>
  Сценарiй гри "<strong><%= link_to @game.name, @game %></strong>"
</h2>

<h3>
  Рівень: <%= @game.levels.map { |level| "<a href=\"#level#{level.position}\">№#{level.position}</a>"}.join(', ').html_safe %>
</h3>

<% @game.levels.each do |level| %>
  <%= render 'level', level: level %>
<% end %>