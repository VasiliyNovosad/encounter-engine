<%= form_for @game,  url: game_path(@game) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= render 'form', f: f %>
  <%= f.submit "Змінити гру", class: 'btn btn-light' %>
  <em><%= link_to "(відмінити)", game_path(@game) %></em>
<% end %>